Configuration:
  status: WARN

  Appenders:
    Console:
      name: CONSOLE
      target: SYSTEM_OUT
      ThresholdFilter:
        level: INFO
        onMatch: ACCEPT
        onMismatch: DENY
      PatternLayout:
        pattern: "[%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t %logger{36}:%L] - %msg%n"
    RollingFile:
      - name: FILE
        ignoreExceptions: false
        fileName: /Users/kyg/data/logs/demo.log
        filePattern: " /Users/kyg/data/logs/$${date:yyyy-MM}/${project.name}-%d{yyyy-MM-dd}-%i.log.gz"
        PatternLayout:
          pattern: "%-5level] %d{yyyy-MM-dd HH:mm:ss.SSS} [%t %logger{36}:%L] - %msg%n"
        Policies:
          SizeBasedTriggeringPolicy:
            size: "128 MB"
        DefaultRolloverStrategy:
          max: 1000

  Loggers:
    Root:
      level: INFO
      AppenderRef:
        - ref: CONSOLE
        - ref: FILE
    Logger:
      - name: com.kangyonggan.uc.mapper
        additivity: false
        level: DEBUG
        AppenderRef:
          - ref: CONSOLE
          - ref: FILE
